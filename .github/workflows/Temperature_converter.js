const kelvin = 293; //forcast today is 293 kelvin
const celsius = kelvin - 273; //Let’s convert Kelvin to Celsius by subtracting 273 from the kelvin variable. Store the result in another variable, named celsius.
let fahrenheit = celsius *(9/5) + 32; //Use the provided formula to assign the correct value to fahrenheit, like so:
fahrenheit = Math.floor(fahrenheit); //Use the .floor() method from the built-in Math object to round down the Fahrenheit temperature. Save the result to the fahrenheit variable.
console.log(`The temperature is ${fahrenheit} degrees Fahrenheit.`);
console.log(`The temperature is ${kelvin} degrees kelvin.`);
console.log(`The temperature is ${celsius} degrees celsius.`);
let Newton = celsius * (33/100)
console.log(`The value  is ${Newton} newton.`);
